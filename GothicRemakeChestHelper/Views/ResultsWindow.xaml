<Window x:Class="GothicRemakeChestHelper.Views.ResultsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rozwiązanie Łamigłówki" Height="500" Width="450"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource bg_main}"
        Foreground="{DynamicResource text_primary}">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Kroki do rozwiązania:" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>

        <ListBox Grid.Row="1" ItemsSource="{Binding solution_steps}" ScrollViewer.VerticalScrollBarVisibility="Auto" Margin="0,0,0,15">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5,8">
                        <TextBlock Text="{Binding move_description}" FontSize="14" TextWrapping="Wrap" Margin="0,0,0,5"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Stan: { " FontSize="14" Foreground="{DynamicResource text_secondary}"/>
                            <ItemsControl ItemsSource="{Binding letters}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding display_text}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                                                    <Setter Property="FontSize" Value="14"/>
                                                    <Setter Property="Foreground" Value="{DynamicResource text_secondary}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding is_changed}" Value="True">
                                                            <Setter Property="Foreground" Value="{DynamicResource accent_color}"/>
                                                            <Setter Property="FontWeight" Value="Bold"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <TextBlock Text=" }" FontSize="14" Foreground="{DynamicResource text_secondary}"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Uruchom tryb Krok po Kroku 🡆" Height="45" FontSize="14" FontWeight="Bold" Margin="0,0,15,0" Background="{DynamicResource accent_color}" Foreground="White" Click="step_mode_button_click"/>

            <Button Grid.Column="1" Content="Zamknij" Height="45" Width="100" FontSize="14" Click="close_button_click"/>
        </Grid>
    </Grid>
</Window>